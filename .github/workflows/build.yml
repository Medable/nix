name: build
on: {workflow_dispatch, push: {branches: [main]}, pull_request}
jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-24.04
          - macos-15
    name: nix (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4.2.2
      - uses: uses: cachix/install-nix-action@v31
      - run: nix --show-trace build -f . custom jacobi_tools
